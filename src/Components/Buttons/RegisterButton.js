import React from "react";
import { PrimaryButton  } from "./styled";

export default function RegisterButton ({title}){

    return(
        <PrimaryButton>
            {title}
        </PrimaryButton>
    )
}